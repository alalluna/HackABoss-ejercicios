package com.hackaboss.holaMundo.models;
//https://www.youtube.com/watch?v=qEGOM8fHszE
//la clase enum se define en el encabezado (en lugar de class es enum)
public enum Rol {
    ROL_ALUMNO,
    ROL_PROFESOR,
    ROL_INVITADO
}